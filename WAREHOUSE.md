# 1인칭 무인 지게차 창고 데모

기본 맵 `/Game/FirstPerson/Lvl_FirstPerson`에 40m × 40m 물류창고를 배치했습니다. 사진을 참고해 중앙 통로 양쪽에 약 5m 높이의 팔레트 랙을 연속 배치하고, 랙마다 3단의 팔레트와 화물을 올렸습니다. 기존 1인칭 플레이어로 돌아다닐 수 있습니다. 지게차 에셋과 이동 기능은 추후 작업 대상이므로, 현재는 크기 확인을 위한 정지된 임시 모델을 배치했습니다.

## 실행

1. Unreal Engine 5.8로 `msc_vr.uproject`를 엽니다. C++ 모듈을 빌드하라는 메시지가 나오면 빌드합니다.
2. `/Game/FirstPerson/Lvl_FirstPerson` 맵을 열고 **플레이**를 누릅니다. 이 맵은 프로젝트의 시작 맵으로 설정돼 있습니다.
3. 랙 사이 통로를 걸으며 충돌과 시야를 확인합니다. 지게차 임시 모델은 움직이지 않습니다.

벽·지붕·화물에는 `BlockAll`, 바닥에는 `BlockAllDynamic` 충돌 프로필을 적용했습니다. Fab 랙 부품처럼 단순 충돌 형상이 없을 수 있는 에셋에는 보이지 않는 박스 충돌체를 함께 배치했습니다. 랙 22칸, 하역 화물 2곳, 천장 조명 6곳에 충돌체가 있습니다. 지게차 임시 모델에도 차체·포크·적재 팔레트를 아우르는 박스 충돌체가 있습니다.

## 에셋과 협업

창고 랙, 팔레트, 상자, 조명, 콘크리트 재질은 Fab의 `Scene_Warehouse` 팩을 참조합니다. 이 대용량 팩은 Git에서 제외돼 있으므로 다른 팀원도 같은 Fab 에셋을 프로젝트의 `Content/Scene_Warehouse` 경로에 설치해야 맵이 정상 표시됩니다. 맵 변경과 지게차 C++ 코드는 Git으로 공유할 수 있습니다.

지게차 메시가 확보되면 `Source/msc_vr/WarehouseForklift.cpp`의 `AddPart` 구성 요소를 교체하면 됩니다. 주행 로직은 이후 별도로 구현합니다.

`Scripts/build_first_person_warehouse.py`는 맵을 다시 배치하는 제작 스크립트입니다. 실행하면 `WH_` 액터를 새로 만들고 기본 템플릿 장애물을 제거하므로, 팀원이 배치한 액터가 있는 맵에서 재실행하기 전에 변경 사항을 확인하세요. 에디터를 열어 둔 상태에서 외부 스크립트로 맵을 갱신했다면 에디터에서 맵을 다시 열어 변경 내용을 로드하세요.
